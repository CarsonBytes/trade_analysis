# secrets
analyst/.env
.env

# data / outputs
*.csv
!eurusd_daily.csv
# cached/serialised datasets (replay caches, etc.) — regenerated locally, not for VC
*.pkl
# generated artifacts (datasets, model dumps, exports) — see artifacts/README.md
artifacts/*
!artifacts/README.md

# python / uv
__pycache__/
*.pyc
.venv/
# keep uv.lock tracked for reproducible installs
# generated CA bundle (machine-specific) — regenerated next to net.py
winca.pem
# local SQLite state (paper trades, api-usage counter) — not for version control
dashboard/dashboard.db
# exported reports/CSVs
exports/
# debug logs
logs/
