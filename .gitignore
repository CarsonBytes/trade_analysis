# secrets
analyst/.env
.env
.env.live

# data / outputs
*.csv
!eurusd_daily.csv
# cached/serialised datasets (replay caches, etc.) — regenerated locally, not for VC
*.pkl
# generated artifacts (datasets, model dumps, exports) — see artifacts/README.md
artifacts/*
!artifacts/README.md

# python / uv
__pycache__/
*.pyc
.venv/
# keep uv.lock tracked for reproducible installs
# generated CA bundle (machine-specific) — regenerated next to net.py
winca.pem
# local SQLite state (paper trades, api-usage counter) — not for version control
dashboard.db
dashboard_live.db
dashboard_mode.db
# 2026-08-26: the only sibling not already covered -- live-updating local state that was
# sitting untracked in dashboard/, one `git add -A` away from being committed.
dashboard_preview.db
# exported reports/CSVs
exports/
# debug logs
logs/
