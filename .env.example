# Copy to .env (already gitignored -- never commit the real file) and fill in the two values
# below with the SAME paper-account credentials the native Windows Gateway uses
# (C:\IBC\config.ini's IbLoginId / IbPassword). Do not put these in docker-compose.yml directly
# -- that file IS tracked in git.
TWS_USERID=
TWS_PASSWORD=
