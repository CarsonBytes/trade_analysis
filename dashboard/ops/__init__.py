"""Operational one-off tools that act on the BROKER (as opposed to dashboard/research/,
which only reads and simulates). Everything in here must default to a dry run and require
an explicit APPLY=1 to send anything."""
