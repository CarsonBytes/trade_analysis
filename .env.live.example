# Copy to .env.live (already gitignored -- never commit the real file) and fill in the two
# values below with the LIVE account's real credentials (C:\IBC-Live\config.ini's IbLoginId /
# IbPassword) -- account U12991898, NEVER paper's DUK968178 credentials. Do not put these in
# docker-compose.live.yml directly -- that file IS tracked in git. Leave blank during Stage 2
# (parallel/stub build verification, no live-account risk); only populate for Stage 3, after
# the explicit go/no-go checkpoint -- a real 2FA phone-approval push will be required the
# moment these are real.
TWS_USERID=
TWS_PASSWORD=
